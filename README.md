Полошков Ярослав Витальевич, группа 220042-11, вариант 4, лабораторная №1

## Отчет по заданиям:

* **Задание 4:** Создан файл `script.py` и добавлен первый коммит. Затем файл изменен и сделан второй коммит.
* **Задание 6:** Создана ветка `feature`, добавлен файл. Выполнено слияние в `main` с флагом `--no-ff` для создания merge-коммита.
* **Задание 10:** Склонирован `https://github.com/octocat/Hello-World.git`. Адрес, команда и анализ сохранены в `repo_history.txt`.
* **Задание 5:** Продемонстрирован `git rebase` ветки `rebase_branch` на `main`. Логи до и после (линеаризация и смена хешей) сохранены в `before_rebase.txt` и `after_rebase.txt`.
* **Задание 9:** Сформирован отчёт `git shortlog` и сохранён в `shortlog.txt`.
